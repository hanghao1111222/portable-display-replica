export const shopifyConfig = {
  // Replace with your actual Shopify store domain (e.g., your-store.myshopify.com)
  domain: "anyking-store.myshopify.com",
  
  // Set to true to route checkout flows to Shopify, false to use the fallback flows
  useShopifyCheckout: true,
};
